# LLM API Configuration (ChatAI is default, but OpenAI is also supported)
OPENAI_API_KEY=your_api_key_here

# Database (Optional - uses in-memory storage by default)
# DATABASE_URL=postgresql://user:password@localhost:5432/ytranslate

# Server Configuration
PORT=3000
NODE_ENV=development

# Storage Configuration (choose one)
# USE_SQLITE_STORAGE=true   # Recommended: Persistent SQLite database
# USE_FILE_STORAGE=true     # Alternative: File-based storage
# Default: In-memory storage (not persistent)